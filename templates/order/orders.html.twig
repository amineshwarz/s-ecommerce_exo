{% extends 'base.html.twig' %}

{% block title %}Hello OrderController!{% endblock %}

{% block body %}
<h1 class="text-center mb-5" style="margin-top:3em;">Historique des commandes</h1>
{{ include ('layouts/_flash_message.html.twig') }}

{% if orders is empty %}
    <div class="alert alert-info text-center" role="alert">
        Vous n'avez pas encore passé de commande.
    </div>
{% endif %}
<div class='order-card text-center'>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Nom</th>
                <th scope="col">Prénom</th>
                <th scope="col">Téléphone</th>
                <th scope="col">Adresse</th>
                <th scope="col">Ville</th>
                <th scope="col">Action</th> 
            </tr>
        </thead>
        <tbody>
            {% for order in orders %}
                <tr>
                    <td>{{ order.firstName }}</td>
                    <td>{{ order.lastName }}</td>
                    <td>{{ order.phone}} </td>
                    <td>{{ order.adresse }}</td>
                    <td>{{ order.city.name }}</td>
                    <td>
                        <a href="#" class="btn btn-sm btn-green" data-bs-toggle="modal" data-bs-target="#productModal{{ order.id }}"> détails</a>
                    </td>   
                </tr>
                {% for product in order.orderproducts %}
                {#------------- modal -------------------#}
{# 
                <div class="modal fade" id="productModal{{ order.id }}" >
                    <div class="modal-dialog modal-dialog-centered modal-lg">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">{{ order.firstName }}</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                            </div>
                            <div class="modal-body d-flex flex-column flex-md-row">
                            
                                    <div>{{ product.product.name }}</div>
                                    <div>{{ product.qte }}</div>
                                    <div>{{ product.product.prix }} €</div>
                                    <div>{{ product.product.prix*product.qte}} €</div>    
                                    <div>
                                        <span> Frais de livraison : {{ order.city.shippingCost }} €</span>
                                        <h5 class='mb-5'> Total de la commande : {{ order.totalPrice }} €</h5> 
                                        {% if order.isCompleted != true %}
                                            <a href="{{ path('app_order_is-completed-update',{'id':order.id}) }}" class="btn btn-sm btn-green">Marquer comme livrée</a> 
                                        {% endif %}
                                        <a href="{{ path('app_order_remove',{'id':order.id}) }}" class="btn btn-sm btn-danger" onclick="return confirm('Es-tu sûr de vouloir supprimer cet élément ?');">Supprimer</a>
                                        <a href="{{ path('app_bill', {'id': order.id}) }}" class="btn btn-sm btn-info">Facture</a>
                                        <a href="#" class="btn btn-sm btn-outline-green"  data-bs-toggle="modal" data-bs-target="#productModal{{ order.id }}">detail</a>
                                    </div>
                                    <div>
                                        <span class='text-primary'> Informations du client :</span>
                                        <h4> commande n°{{ order.id }}</h2>
                                        {% if order.isCompleted == true %}
                                            <span class="badge bg-success mb-3">Commande livrée</span>
                                        {% else %}
                                            <span class="badge bg-danger mb-3">Commande non livrée</span>
                                        {% endif %}
                                        <br>
                                    </div>
                                
                            </div>
                        </div>
                    </div>
                </div>             #}

                <div class="modal fade" id="productModal{{ order.id }}" tabindex="-1" aria-hidden="true">
                    <div class="modal-dialog modal-dialog-centered modal-lg">
                        <div class="modal-content shadow-lg border-0 rounded-4">
                            
                            <!-- Header -->
                            <div class="modal-header bg-dark text-white rounded-top-4">
                                <h5 class="modal-title">
                                    Commande de <span class="fw-bold">{{ order.firstName }}</span>
                                </h5>
                                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                            </div>

                            <!-- Body -->
                            <div class="modal-body p-4">
                                <div class="row g-4">
                                    
                                    <!-- Infos Produits -->
                                    <div class="col-md-7">
                                        <h6 class="text-uppercase text-secondary fw-bold">Détails du produit</h6>
                                        <div class="card border-0 shadow-sm p-3 mb-3">
                                            <div class="d-flex justify-content-between">
                                                <span class="fw-semibold">{{ product.product.name }}</span>
                                                <span class="text-muted">x {{ product.qte }}</span>
                                            </div>
                                            <div class="d-flex justify-content-between mt-2">
                                                <span>Prix unitaire :</span>
                                                <span class="fw-bold">{{ product.product.prix }} €</span>
                                            </div>
                                            <div class="d-flex justify-content-between mt-2 border-top pt-2">
                                                <span>Total produit :</span>
                                                <span class="fw-bold text-success">{{ product.product.prix*product.qte}} €</span>
                                            </div>
                                        </div>

                                        <div class="d-flex justify-content-between">
                                            <span> Frais de livraison : </span>
                                            <span class="fw-bold">{{ order.city.shippingCost }} €</span>
                                        </div>
                                        <h5 class="mt-3">
                                            Total Commande : <span class="text-primary">{{ order.totalPrice }} €</span>
                                        </h5>
                                    </div>

                                    <!-- Infos Client -->
                                    <div class="col-md-5">
                                        <h6 class="text-uppercase text-secondary fw-bold">Informations Client</h6>
                                        <div class="border rounded p-3 shadow-sm bg-light">
                                            <p class="mb-1"><strong>Commande n° :</strong> {{ order.id }}</p>
                                            {% if order.isCompleted %}
                                                <span class="badge bg-success">Commande livrée</span>
                                            {% else %}
                                                <span class="badge bg-danger">Commande non livrée</span>
                                            {% endif %}
                                        </div>

                                        <!-- Boutons Actions -->
                                        <div class="d-grid gap-2 mt-4">
                                            {% if order.isCompleted != true %}
                                                <a href="{{ path('app_order_is-completed-update',{'id':order.id}) }}" 
                                                class="btn btn-success btn-sm">
                                                    ✅ Marquer comme livrée
                                                </a>
                                            {% endif %}

                                            <a href="{{ path('app_order_remove',{'id':order.id}) }}" 
                                            class="btn btn-danger btn-sm"
                                            onclick="return confirm('Es-tu sûr de vouloir supprimer cet élément ?');">
                                                🗑 Supprimer
                                            </a>

                                            <a href="{{ path('app_bill', {'id': order.id}) }}" 
                                            class="btn btn-info btn-sm text-white">
                                                📄 Facture
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>

                {% endfor %}
            {% endfor %}
        </tbody>
    </table>

    <div class=" mx-5 justify-content-center">
        {{ knp_pagination_render(orders, 'layouts/paginator.html.twig') }}
    </div>
</div>
 
{% endblock %}
